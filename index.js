//this is my first javascript code
// variable
let name = 'anubhav';
console.log(name);

let firstName = 'Anubhav';
let lastName = 'Ajmera';

console.log(firstName + " " + lastName);

//constant 
const number = 0.3;
console.log(number);

//primitive data types
let name1 = 'Anubhav'; // string literal
let age = 30;          // number literal
let isApproved = false; //boolean literal
let secondName = undefined;
let selectedColor = null;

console.log(name1, age, isApproved, secondName, selectedColor);

console.log(typeof(name1), typeof(age), typeof(isApproved), typeof(secondName), typeof(selectedColor))
